export default (debug, rooms, room) => {
  // if (debug) console.log(`[debug] [on] [create-room] (${room})`);
  
  // if (!room.startsWith('game-')) {
  //   if (debug) console.log(`[debug] [warning] Ignored room because it does not start with 'game-'`);
  //   return;
  // }

  // rooms.push({ id: room, status: 'waiting' });

  // console.log(`[ws] [create-room] ${room} created as room`);
};
